{
    "actorOutputSchemaVersion": 1,
    "title": "Output schema of BLS Occupation Wage Lookup",
    "description": "One record per occupation lookup: employment count, annual/hourly mean and median wage.",
    "properties": {
        "results": {
            "type": "string",
            "title": "Results",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
